{
    "name": "niivue-bet",
    "private": true,
    "version": "0.0.0",
    "scripts": {
      "dev": "./fetch_dcm2niix.sh && vite",
      "build": "./fetch_dcm2niix.sh && vite build && cp dicom2nifti.html.mem dist/assets/",
      "preview": "vite preview",
      "deploy": "npm run build && cp *.gz dist"
    },
    "devDependencies": {
      "gh-pages": "^3.2.3",
      "vite": "^2.9.0"
    },
    "dependencies": {
      "@niivue/niivue": "^0.22.2"
    }
  }
  